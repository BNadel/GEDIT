usage Rscript DeconRNASeq.R mixture.tsv reference.tsv

mixture.tsv and reference.tsv cannot have duplicate row names

commands to import necessary packages:

source("https://bioconductor.org/biocLite.R")
biocLite("DeconRNASeq")
